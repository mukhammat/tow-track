import { drizzleClient, partners } from "@db"
import { CreatePartnerDto } from "."
import { eq } from "drizzle-orm";

export interface IPartnerService {
}

export class PartnerService implements IPartnerService {
    constructor() {}
}